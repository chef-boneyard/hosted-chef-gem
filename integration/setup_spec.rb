require File.expand_path("../spec_helper.rb", __FILE__)

describe "hosted-chef setup" do
  describe "when given a valid username, orgname, and password" do

    before(:all) do
      #run the codes
    end

    it "configures knife.rb" do
      pending
    end

    it "fetches a valid username.pem" do
      pending
    end

    it "fetches a valid validation.pem" do
      pending
    end

  end
end

